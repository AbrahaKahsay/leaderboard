const scoreURL = `https://us-central1-js-capstone-backend.cloudfunctions.net/api/games/${id}/scores/`;
const addScore = async (object) => {
    await fetch(scoreURL, {
        method: 'POST',
        headers: {
            'Content-type':'application/json'
        },
        body: object,
    });
}

const getScores = async () => {
    const scores = await fetch(scoreURL);
    return scores.json();
};



export {
    addScore, getScores, 
}